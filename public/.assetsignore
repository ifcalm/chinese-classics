# 兜底：线上 /content/* 由 Worker 代理 R2，这 11.7 万文件不该也不能作为静态资产上传(2 万文件上限)。
# 2026-09-13 起 public/ 下已无 content（dev 改由 vite.config.ts 的 dev-content 插件供给），
# dist/content 正常不会出现；此条留作防回归——万一谁又把软链放回 public/，仍挡得住。
content
