node_modules
dist
dist-content
dist-ssr
*.local
.DS_Store
.vite

# TypeScript / 构建产物
# tsconfig.node 的产物已改投 node_modules/.tmp/tsconfig-node（见 tsconfig.node.json）——
# 根目录留 vite.config.js 会 shadow 掉 .ts。下两条留作防回归。
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea

# 本地内容(由 build-content 生成；dev 的 /content/* 由 vite.config.ts 的 dev-content 插件供给。
# public/content 软链已于 2026-09-13 移除——vite 会把它实体拷进 dist，白拷 817MB)
public/content

# R2 上传临时记录
.r2-*.log
.r2-*.txt

# R2 上传缓存(本地，按内容哈希增量；丢了重传即可)
.r2-uploaded.json
.wrangler
.ws-cache/
# Python 字节码缓存（原为 scripts/__pycache__/，不匹配嵌套子目录，
# scripts/quanzhen/__pycache__/ 曾因此被误提交）
__pycache__/
*.pyc

# CBETA 底本 XML：公开可复得（cbeta-org/xml-p5），T 号记于 parse-cbeta.mjs 与校勘日志，不入库
scripts/cbeta/

# 殆知阁底本 txt：公开可复得（garychowcmu/daizhigev20），书名与路径记于 parse-zhaji.py，不入库
scripts/dzg/
